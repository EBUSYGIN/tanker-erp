import { Button } from "../../shared/ui";
import styles from "./Header.module.css";

export const Header = () => {
  return (
    <div className={styles.header}>
      {/* <Button size="s" appearance="primary" icon="Bell" border="border-ghost" />
      <Button size="s" appearance="primary" icon="Mail" border="border-ghost" /> */}
    </div>
  );
};
